# 猫卷 UI/交互/动画 视觉审查报告

- 审查方式：真机（NX729J · Android 15 · 1116×2480 @480dpi）安装正式版 APK `猫卷_v1.0.2_新logo桌面图标_20260820.apk`，逐屏截图 + 录屏 + 代码交叉核验
- 审查日期：2026-08-22
- 截图目录：`docs/ui_review_assets/`（45+ 张）
- 状态说明：主题专项完整覆盖；刷题/解析/统计/设置/我的/关于完整覆盖；**未覆盖**：管理题库页、导入页、练习/背题模式实机流（设备交互坐标受限，见 §5 附注）

---

## 一、页面布局与视觉（分类 A）

| # | 截图 | 严重度 | 问题 | 证据 |
|---|---|---|---|---|
| A1 | 49/50/51 | **高** | **AI 解析内容 Markdown 未渲染**：`**加粗**` 星号与 `- ` 列表符号原样显示 | `lib/widgets/ai_response_widget.dart` 使用 `MarkdownBody`，但真机显示原始语法（大概率是 flutter_markdown 解析失败/未生效，需修复轮定位：排查依赖版本与 `\r\n`/转义链） |
| A2 | 59 | 中 | 统计页「年度坚持」三列月历可读性差：格子数字偏小（11–13px）、三列紧贴无分隔、月/年标签跨列视觉粘连（"2026年 8月"） | `stats_tab.dart:242` `cellH = (w/7/0.95).clamp(0,56)`；`monthly_calendar.dart:56-63` 月/年两个独立 Text |
| A3 | 59/60 | 中 | 趋势图数据可视性差：`chartMax = max(100, maxTotal*1.15)`（`trend_chart.dart:108`）使 2–30 题的小量柱仅数像素高，近 30 天图看起来"空白" | 当天已刷 2 题但图中无可见柱 |
| A8 | 42/48/49/50/51 | 中 | 答题页底部「上一题」按钮文字竖排 3 行（"上/一/题"），按钮宽度不足 | quiz_screen.dart 底部操作 3 胶囊布局，窄屏挤压 |
| D6 | 42 | 中 | 答题反馈语义色不通用：**正确=粉紫色/错误=橙色**（碧海主题 success/danger 语义色），与用户"绿对红错"心智不一致；建议定义并全局使用 success=绿、danger=红 | `theme_service.dart` AppThemeColors.success/danger；见截图 42 |
| A10 | 39 | 中 | 浅色闪屏下 logo 白底与白背景融合（几乎只见文字不见图标框）；深色闪屏正常（01） | `assets/app_logo.png` 白底；闪屏白底 → 建议闪屏用主题色底或 logo 加描边 |
| A10b | 55/37 | 低 | 首页核心指标数字偏小："2 道 / 0%" 主数字约 32px，冲击力不足 | 建议 44–56px 大字号 |
| A5 | 41 | 低 | 未作答时答题页下半屏大片空白（无操作提示/占位） | 答后出现反馈与按钮，未答时下方空 |
| A6 | 05/06/09 | 低 | 设置页「导出日志文件」按钮文字换行"导出日/志文件" | 按钮宽度不足 |
| A7 | 04/68 | 低 | AI 配置页「API 地址」输入框长文本截尾显示 `...completior`（视觉截断） | 单行输入无滚动提示 |
| A9 | 37/55 | 低 | 首页月历热力图浅色下"少/达标/多"色阶区分度低（均为浅蓝系） | `monthly_calendar.dart:38-39` 用 `withOpacity` 浅化 |
| A11 | 56 | 低 | 快速操作「错题本」副标题「使用 FSRS 算法全权生成」用词生硬 | 建议「仅显示当前应复习的错题」 |
| A12 | 61 | 低 | 「我的」页下半屏大面积空白 | 入口只有 3 项 |

## 二、主题与深色模式（分类 B）

覆盖：浅色 5 套（23 护眼/24 品牌/21 极简/25 星际/26 碧海）+ 深色 5 套（30-34）+ 跟随系统（35/36 双向验证通过）。

| # | 严重度 | 问题 |
|---|---|---|
| B1 | 低 | 主题选择仅文字 radio，无色彩预览色块，用户切换成本高 |
| B2 | 低 | 「调试日志」卡片表面色（粉米）在星际穿越浅色主题下与页面米色背景对比微弱（19） |
| B3 | — | 深色下系统原生控件（日期/时间选择器）未实机验证（标注待验） |
| 结论 | — | **深色 5 变体整体协调**，文字/卡片/radio 对比可读；跟随系统双向生效 ✓ |

## 三、动画与转场（分类 C）

- 真机录屏受设备 screenrecord 限制未能完成提帧；以代码参数 + 静态帧交叉核验：
- C1 低：闪屏固定 2 秒（splash 停留）略长，建议 1.2–1.5s
- C2 低：答题反馈 200ms 颜色过渡（quiz_screen.dart:584）+ 答对 600ms 自动跳题（:867）——节奏整体合理，600ms 对"答案消化"偏快，建议 800–1000ms 或答对跳题时增加进度条提示
- C3 低：解析弹层 400ms easeInOut（:288）+ 300ms AnimatedSwitcher（:328）——OK
- C4 低：切 Tab 无动画（IndexedStack 瞬切）——可接受

## 四、交互细节（分类 D）

| # | 截图/证据 | 严重度 | 发现 |
|---|---|---|---|
| D1 | 41 | 中 | 答题页顶部「铅笔/眼睛」图标无 tooltip/文字，功能语义不明（跳题/查看解析） |
| D2 | 54 | ✓ | **退出刷题确认弹窗设计正确**：明确"进度将保留，可下次从首页继续"+ 继续/退出双按钮 |
| D3 | 64 | ✓ | 未选题库时点「定向爆破」Snackbar 引导“请先在「管理题库」中选择要刷的题库” ✓ |
| D4 | 55/38 | ✓ | 断点续刷卡实时同步进度（已答 1/154→2/154） ✓ |
| D5 | 42 | 低 | 「正确答案: D / 你的答案: C」字号偏小（15px），建议加粗+图标 |
| D6 | 22/26 | ✓ | 主题切换即时生效、设置页各处反馈正常 ✓ |
| — | 02 等 | ✓ | 切 Tab 后统计刷新、数据即时更新（2道→27题 22.2%） ✓ |

## 五、未覆盖项与设备限制说明

1. **管理题库页 / 导入页 / 练习·背题模式实机流**：本轮 adb tap 与真机布局存在系统级偏移（视觉中心与响应区不一致），多次校准未稳定命中；已通过代码 + 既有 99 项自动化测试佐证功能正确性，建议下一轮用 uiautomator 语义桥接后补测。
2. 深色模式下日期/时间选择器（系统原生样式）未单独验证。
3. 录屏（screenrecord）在本机设备不可用（adb path 转换 + 设备限制），动画以代码参数为准。

## 六、修复优先级建议

| 优先级 | 项 | 建议修法要点 |
|---|---|---|
| P0 | A1 Markdown 未渲染 | 排查 ai_response_widget 解析链（`\r\n`、零宽转义、包版本），确保 ** 与列表真实渲染；补一条真机回归用例 |
| P1 | D6 对错语义色 | success/danger 语义色统一为绿/红（全主题），答题反馈/统计图表一致 |
| P1 | A3 趋势图轴 | chartMax 自适应：`max(ceil(dataMax*1.15), 10)` 下限抬高小值可见性 |
| P2 | A8 上一题按钮 | 操作栏改为「上/下一题」等宽双胶囊 + 防换行 |
| P2 | A2 三列月历 | 列间加分隔/间距，数字 13px+，标签对齐 |
| P2 | A10 浅色闪屏 | 闪屏底色随主题（深色变体下用深底），或 logo 加主题色描边 |
| P3 | B1 主题色预览 | radio 行加 2–3 个色点预览 |
| P3 | A5/A12/A6/A7/A9/A11/A12/C1/C2 | 按上表逐项微调 |

---
报告完。修复需另开启一轮（先批准修复计划）。
