# 安全政策

## 报告安全漏洞

如果你发现了猫卷的安全漏洞，**请不要通过公开的 Issue 报告**。

请通过以下渠道私下联系：

- GitHub 私信：[@laow07573-web](https://github.com/laow07573-web)
- 邮箱：<!-- TODO: 填写你的联系邮箱后删除本行 -->（待补充）

我会在 **72 小时内**确认收到，并在修复完成后酌情公开致谢（如你愿意署名）。

## 适用范围

本政策适用于：

- 猫卷客户端（Android / Windows）及其发布产物
- 本仓库中的源代码

## 安全设计说明（摘要）

- 所有学习数据（题库、错题、批注、进度）存储于设备本地 SQLite 数据库，不上传云端
- 无账号体系，不采集任何个人信息
- API Key 使用 AES-256-GCM 加密存储，Android 端优先存入系统 Keystore
- AI 请求由用户自己的 Key 直连模型服务商，不经过本项目任何服务器
- 备份导出默认**不包含 API Key**；如需包含，须设置备份口令并对备份文件整体加密
- 发布产物提供 SHA-256 校验值（见各 Release 资产中的 `SHA256SUMS.txt`）
- 构建开启代码混淆，并内置 APK 签名校验（二次打包将拒绝启动）

## 密钥与凭据的保管约定

对维护者同样适用的硬性约定：

- **签名文件与口令永不入库**：`android/key.properties`、`**/*.jks`、`**/*.keystore`
  均已在 `.gitignore` 中排除；口令只离线保管，不写进任何文档或脚本
- **发布凭据不落盘**：GitHub Token / Cloudflare Token 只以环境变量传入，
  不写进仓库文件；CI 侧使用 Repository Secrets
- **符号文件不随发布**：混淆构建的符号文件（`build/symbols`）只作归档，
  不上传 Release（否则可被用于反混淆）

## 给用户的建议

- 请从本仓库的 **Releases** 页面下载，不要使用第三方转载的安装包
- 下载后建议核对 SHA-256：
  - Windows：`Get-FileHash .\MaoJuan-*.exe -Algorithm SHA256`
  - Android：使用任意哈希校验工具
- 带 API Key 的备份文件请妥善保管（它是加密的，但口令强度取决于你自己），
  避免通过公开渠道传输

## 已知边界

- 静态扫描与签名校验只能覆盖「已扫描范围」，不能承诺绝对不存在漏洞
- 第三方 AI 服务商的数据处理策略不属于本项目控制范围，请阅读对应服务商的隐私条款
